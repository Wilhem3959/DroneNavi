using Microsoft.UI.Xaml.Controls;

using NavDrone.ViewModels;

namespace NavDrone.Views;

public sealed partial class MainPage : Page
{
    public MainViewModel ViewModel
    {
        get;
    }

    public MainPage()
    {
        ViewModel = App.GetService<MainViewModel>();
        InitializeComponent();
    }
}
